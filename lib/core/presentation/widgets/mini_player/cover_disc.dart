import 'package:flutter/material.dart';

class CoverDisc extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return AspectRatio(
        aspectRatio: 1,
        child: Container(
          color: Colors.red,
        ));
  }
}
